#!/usr/bin/env python3
"""transpose a 2D matrix"""


def matrix_transpose(matrix):
    """
    matrix_transpose: returns the transpose of a 2D matrix
    
    Returns: the transpose of a 2D matrix
    """
    for index in len(matrix[0]):
        for value in index:
            print(value)
